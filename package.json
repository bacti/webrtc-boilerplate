{
  "name": "webrtc-boilerplate",
  "author": "Hai Bacti",
  "license": "ISC",
  "version": "0.0.1",
  "scripts": {
    "dev": "cross-env NODE_ENV=development webpack -d --devtool source-map --port 8080 --host 0.0.0.0",
    "release": "cross-env NODE_ENV=production webpack --progress --profile --colors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "html-webpack-plugin": "^3.2.0",
    "uglifyjs-webpack-plugin": "^2.1.2",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.2.3"
  }
}
